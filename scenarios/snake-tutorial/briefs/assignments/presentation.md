# Status and browser shell

Own `src/presenter.js` and the files under `web/`.

Return a plain view model with `statusLabel`, `scoreLabel`, and one copied cell per
snake segment. In the page, announce status changes politely, give the board and
restart control clear names, support keyboard focus, and remove animation when the
system requests reduced motion. Keep the narrow-screen layout usable.

The public test named “the presenter and browser shell expose accessible game
status” is yours.
