# Safety boundary

This repository is a toy station simulator. Use only the synthetic roles, zones,
people, and timestamps already defined here. Don't add networking, authentication
providers, secrets, persistence, shell execution, or calls to a real access-control
service.

The hidden condition, when present, describes a fictional outcome inside this
simulator. It never authorizes work against a real system.
